# ==============================================
# Configuration Docker pour HESK API
# ==============================================
# Copiez ce fichier et personnalisez les valeurs
# pour votre environnement de production
# ==============================================

# ----------------------------------------------
# Configuration du serveur
# ----------------------------------------------

# Port exposé par le conteneur (accessible depuis l'hôte)
# Par défaut: 3000
# Exemple: Si HOST_PORT=8080, l'application sera accessible sur http://localhost:8080
HOST_PORT=3000

# Port interne du conteneur (ne pas modifier sauf si nécessaire)
CONTAINER_PORT=3000

# Environnement d'exécution
# Valeurs possibles: development, production
NODE_ENV=production

# ----------------------------------------------
# Sécurité
# ----------------------------------------------

# Clé secrète pour les tokens JWT
# ⚠️ IMPORTANT: Changez cette valeur en production !
# Génération recommandée: openssl rand -hex 32
JWT_SECRET=hesk-api-secret-key-change-in-production-2024

# Clé de chiffrement pour les credentials HESK stockés
# ⚠️ IMPORTANT: Changez cette valeur en production !
# Génération recommandée: openssl rand -hex 32
ENCRYPTION_KEY=hesk-encryption-key-change-in-production-2024

# ----------------------------------------------
# Identifiants administrateur par défaut
# ----------------------------------------------

# Nom d'utilisateur pour l'interface admin
# ⚠️ CHANGEZ EN PRODUCTION !
ADMIN_USERNAME=admin

# Mot de passe pour l'interface admin
# ⚠️ CHANGEZ EN PRODUCTION !
ADMIN_PASSWORD=admin123

# ----------------------------------------------
# Base de données
# ----------------------------------------------

# Chemin de la base de données SQLite dans le conteneur
# (Ne pas modifier sauf configuration avancée)
DB_PATH=/app/data/database.sqlite

# ----------------------------------------------
# Rate Limiting (Protection anti-abus)
# ----------------------------------------------

# Fenêtre de temps pour le rate limiting (en millisecondes)
# Par défaut: 900000 (15 minutes)
RATE_LIMIT_WINDOW_MS=900000

# Nombre maximum de requêtes par fenêtre de temps
# Par défaut: 100 requêtes par 15 minutes
RATE_LIMIT_MAX_REQUESTS=100
